import { FilterlistacursosPipe } from './filterlistacursos.pipe';

describe('FilterlistacursosPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterlistacursosPipe();
    expect(pipe).toBeTruthy();
  });
});

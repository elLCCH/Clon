import { FilterTeoricasPipe } from './filter-teoricas.pipe';

describe('FilterTeoricasPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTeoricasPipe();
    expect(pipe).toBeTruthy();
  });
});

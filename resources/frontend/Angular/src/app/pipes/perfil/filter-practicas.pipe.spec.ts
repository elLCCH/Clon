import { FilterPracticasPipe } from './filter-practicas.pipe';

describe('FilterPracticasPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPracticasPipe();
    expect(pipe).toBeTruthy();
  });
});

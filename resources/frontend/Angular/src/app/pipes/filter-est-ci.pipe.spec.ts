import { FilterEstCiPipe } from './filter-est-ci.pipe';

describe('FilterEstCiPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEstCiPipe();
    expect(pipe).toBeTruthy();
  });
});

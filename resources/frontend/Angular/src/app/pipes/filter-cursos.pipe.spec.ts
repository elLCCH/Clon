import { FilterCursosPipe } from './filter-cursos.pipe';

describe('FilterCursosPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCursosPipe();
    expect(pipe).toBeTruthy();
  });
});
